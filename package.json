{
  "name": "jericho-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix apps/api install",
    "build": "cd apps/api && npx nest build",
    "start": "node apps/api/dist/main.js"
  }
}