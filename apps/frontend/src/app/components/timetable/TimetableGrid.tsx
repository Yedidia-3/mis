

// Timetable generator UI removed. This component is a lightweight placeholder
// so legacy imports compile. Use the Timetable Upload screen instead.
export function TimetableGrid() {
  return (
    <div className="p-6">
      <h3 className="text-lg font-semibold">Timetable generation removed</h3>
      <p className="text-sm mt-2 text-gray-600">The in-app timetable generator has been retired. Please upload a workbook from the Timetable → Upload screen, which provides global and class views for users.</p>
    </div>
  );
}
