import 'reflect-metadata';

// Timetable module removed; seed-timetable is deprecated and intentionally
// disabled to avoid referencing removed timetable entities.
console.warn('seed-timetable: timetable module removed; this script is deprecated.');
process.exit(0);
